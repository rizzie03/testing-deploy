import { combineReducers } from "redux";

const rootReducer = combineReducers({
  account: "ini ganti sama reducernya apa", // ini untuk menampung user yang sedang login + simpan token (punya bang zain),
  events: "menampung data2 events + kalau search dan ketemu bakal masuk sini", // ini punya destin dan channy(siap2 conflict wkwkwkw) (btw data2 events ini termasuk ke dalamnya ada comments juga ya tin)
  
});

export default rootReducer;
